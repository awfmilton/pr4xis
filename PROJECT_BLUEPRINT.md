# PROJECT_BLUEPRINT.md — Agent-Ready Configuration
> Auto-generated by kôdex (MCP Config Manager). Do not edit manually.
> Last updated: 2026-04-25T08:55:39.317Z

## Project: pr4xis

## MCP Servers
No MCP servers configured.

## Required Environment Variables
- `CARGO_TARGET_DIR` — Override build artifact directory (speeds up CI caching)
- `RUSTFLAGS` — Compiler flags (e.g. target features, linker overrides)
- `CODECOV_TOKEN` — Authenticate coverage upload to Codecov
- `GITHUB_TOKEN` — GitHub Pages deployment and release creation
- `CARGO_REGISTRY_TOKEN` — Publish crates to crates.io
- `DIRENV_WARN_TIMEOUT` — Suppress slow-shell warnings in the Nix dev environment

## Test Commands
- `cargo test --workspace` — full test suite across all crates
- `cargo check --quiet` — compilation check without artifacts
- `cargo clippy --quiet -- -D warnings` — lint gate (warnings are errors)
- `treefmt --fail-on-change` — formatting gate

## AI Agent Status
- Gemini CLI: ❌ not found
- Jules CLI: ❌ not found
- Claude Code: ❌ not found

## Security Rules
- `crates/core` must remain `no_std + alloc` — never introduce `std` imports; verify any new dependency supports `no_std` before adding it
- All Clippy warnings are treated as errors (`-D warnings`); do not suppress with `#[allow(...)]` without a documented reason
- All dependencies are pinned via `Cargo.lock`; do not introduce floating dependency versions
- The only external network surface is `pr4xis update` (knowledge-base data fetch); no authentication tokens may be stored on disk by the core engine
- GitHub Actions secrets (`CODECOV_TOKEN`, `GITHUB_TOKEN`, `CARGO_REGISTRY_TOKEN`) are scoped to workflow jobs and must never be written to disk or echoed in logs
- Lemon meta annotations must be applied to every structural entity; category-theory naming conventions (morphisms, functors, adjunctions, natural transformations) must follow established `define_ontology!` macro patterns

## Coordination Protocol
This project uses a three-agent orchestration model. Read `TRIAGE_WORKFLOW.md` before starting any non-trivial task — it defines role assignments (Orchestrator, Researcher, Implementer), the triage decision tree (Tier 1 < 500 chars, Tier 2 500–3,000 chars, Tier 3 > 3,000 chars), and the Jules Watch Monitor protocol. See `AI CLI Reference Guide.md` for CLI command syntax for each agent role. The Orchestrator must delegate all codebase exploration to the Researcher to preserve its token budget; the Implementer receives work via GitHub Issues labeled `jules`. Every `.rs` and `.toml` edit is intercepted by the pre-edit enforcement hook — consult the Triage Hook section of `CLAUDE.md` before any file modification.
